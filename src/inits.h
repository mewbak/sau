// Auto-generated file.
extern int adfInit(format*);
extern int aggInit(format*);
extern int bdxInit(format*);
extern int bmcInit(format*);
extern int bmpInit(format*);
extern int borInit(format*);
extern int boxInit(format*);
extern int brigInit(format*);
extern int btbInit(format*);
extern int bytesInit(format*);
extern int c2eInit(format*);
extern int camInit(format*);
extern int ccInit(format*);
extern int celInit(format*);
extern int cl2Init(format*);
extern int cmpInit(format*);
extern int darksealInit(format*);
extern int datInit(format*);
extern int dc6Init(format*);
extern int dccInit(format*);
extern int ddsInit(format*);
extern int defInit(format*);
extern int df2Init(format*);
extern int dirInit(format*);
extern int divinityInit(format*);
extern int domInit(format*);
extern int dsaInit(format*);
extern int epfInit(format*);
extern int flxInit(format*);
extern int foxInit(format*);
extern int framesInit(format*);
extern int fshInit(format*);
extern int gifInit(format*);
extern int gorInit(format*);
extern int groupInit(format*);
extern int grpInit(format*);
extern int hInit(format*);
extern int h4rInit(format*);
extern int heimdallInit(format*);
extern int hrsInit(format*);
extern int idxInit(format*);
extern int ilbInit(format*);
extern int impInit(format*);
extern int itxInit(format*);
extern int keyInit(format*);
extern int lbmInit(format*);
extern int lbxInit(format*);
extern int lgInit(format*);
extern int libInit(format*);
extern int lodInit(format*);
extern int majInit(format*);
extern int monInit(format*);
extern int mpqInit(format*);
extern int mqdbInit(format*);
extern int mulInit(format*);
extern int ndsInit(format*);
extern int obmInit(format*);
extern int p10Init(format*);
extern int pakInit(format*);
extern int palInit(format*);
extern int pckInit(format*);
extern int pcxInit(format*);
extern int pngInit(format*);
extern int pvrInit(format*);
extern int pzxInit(format*);
extern int rawInit(format*);
extern int remapInit(format*);
extern int resInit(format*);
extern int rgmystusInit(format*);
extern int rncInit(format*);
extern int scanInit(format*);
extern int sndInit(format*);
extern int splInit(format*);
extern int sprInit(format*);
extern int ssaaInit(format*);
extern int tgaInit(format*);
extern int tgwInit(format*);
extern int timInit(format*);
extern int tlbInit(format*);
extern int tm2Init(format*);
extern int tryInit(format*);
extern int uopInit(format*);
extern int vhInit(format*);
extern int vidInit(format*);
extern int vsrInit(format*);
extern int warInit(format*);
extern int whizzInit(format*);
extern int xuaInit(format*);
static void *Inits[] = {
  adfInit,
  aggInit,
  bdxInit,
  bmcInit,
  bmpInit,
  borInit,
  boxInit,
  brigInit,
  btbInit,
  bytesInit,
  c2eInit,
  camInit,
  ccInit,
  celInit,
  cl2Init,
  cmpInit,
  darksealInit,
  datInit,
  dc6Init,
  dccInit,
  ddsInit,
  defInit,
  df2Init,
  dirInit,
  divinityInit,
  domInit,
  dsaInit,
  epfInit,
  flxInit,
  foxInit,
  framesInit,
  fshInit,
  gifInit,
  gorInit,
  groupInit,
  grpInit,
  hInit,
  h4rInit,
  heimdallInit,
  hrsInit,
  idxInit,
  ilbInit,
  impInit,
  itxInit,
  keyInit,
  lbmInit,
  lbxInit,
  lgInit,
  libInit,
  lodInit,
  majInit,
  monInit,
  mpqInit,
  mqdbInit,
  mulInit,
  ndsInit,
  obmInit,
  p10Init,
  pakInit,
  palInit,
  pckInit,
  pcxInit,
  pngInit,
  pvrInit,
  pzxInit,
  rawInit,
  remapInit,
  resInit,
  rgmystusInit,
  rncInit,
  scanInit,
  sndInit,
  splInit,
  sprInit,
  ssaaInit,
  tgaInit,
  tgwInit,
  timInit,
  tlbInit,
  tm2Init,
  tryInit,
  uopInit,
  vhInit,
  vidInit,
  vsrInit,
  warInit,
  whizzInit,
  xuaInit,
  0};
